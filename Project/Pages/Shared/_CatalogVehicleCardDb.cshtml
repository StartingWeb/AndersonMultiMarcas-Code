@model Project.Pages.CatalogoModel.CatalogVehicleItem

<article class="vehicle-card catalog-card">
    <a asp-page="/Veiculo" asp-route-id="@Model.Id" class="card-link-cover" aria-label="Abrir detalhes de @Model.Titulo"></a>

    <div class="vehicle-media">
        <img src="@Model.ImageUrl"
             alt="@Model.Titulo"
             class="vehicle-thumb"
             loading="lazy" />
        <span class="vehicle-tag">@Model.Tag</span>
    </div>

    <div class="vehicle-body">
        <div class="catalog-brandline">@Model.Marca • @(Model.Ano?.ToString() ?? "Ano não informado")</div>
        <div class="vehicle-name">@Model.Titulo</div>

        @if (!string.IsNullOrWhiteSpace(Model.Highlight))
        {
            <div class="catalog-highlight-text">@Model.Highlight</div>
        }

        <div class="catalog-specs">
            <span>@Model.Cambio</span>
            <span>@Model.Combustivel</span>
        </div>

        <div class="vehicle-meta">
            @(Model.Quilometragem.HasValue ? $"{Model.Quilometragem.Value:N0} km rodados" : "Quilometragem não informada")
        </div>

        <div class="vehicle-price">
            @(Model.Preco.HasValue ? Model.Preco.Value.ToString("C0", new System.Globalization.CultureInfo("pt-BR")) : "Consulte")
        </div>

        @if (Model.PrecoDe.HasValue)
        {
            <div class="catalog-price-old">De @Model.PrecoDe.Value.ToString("C0", new System.Globalization.CultureInfo("pt-BR"))</div>
        }

        <div class="catalog-actions d-grid gap-2">
            <a href="@Model.WhatsappUrl" target="_blank" class="btn btn-outline-brand w-100">Tenho interesse</a>
        </div>
    </div>
</article>
